# Placeholder only. Jenkins copies POSTGRES_PASSWORD from the Postgres secret.
apiVersion: v1
kind: Secret
metadata:
  name: student-management-backend-secret
  namespace: student-management
type: Opaque
stringData:
  DB_PASSWORD: "INJECTED_BY_JENKINS"
